CREATE TABLE TEAHSTUD(
    KSP INT NOT NULL,
    SURNAME VARCHAR(10) NOT NULL,
    NAME VARCHAR(7) NOT NULL,
    PATRONYMIC VARCHAR(10) NOT NULL,
    Teacher VARCHAR(15) NOT NULL,
    STUDENT VARCHAR(20)NOT NULL,
    STATYS VARCHAR(10),
    FAKYLTET VARCHAR(30) NOT NULL,
    DOLGNOST VARCHAR(30) NOT NULL,
    PRIMARY KEY(KSP)
    );
CREATE TABLE BZNOSU(
    BZN INT NOT NULL,
    KPS INT NOT NULL,
    DATA DATE,
    SYM VARCHAR(100) NOT NULL,
    PRIMARY KEY(BZN)
    );
CREATE TABLE DOLGNOST (
    DOLGNOST  INT NOT NULL,
    teacher INT NOT NULL,
    PRIMARY KEY(DOLGNOST )
    );
CREATE TABLE STATYS (
    STATYS  INT NOT NULL,
    teacher INT NOT NULL,
    PRIMARY KEY(STATYS )
    );
CREATE TABLE FAKYLTET(
    FAKYLTET  VARCHAR(30) NOT NULL,
    PHONE INT NOT NULL,
    DEKAN VARCHAR(30) NOT NULL,
    PRIMARY KEY(FAKYLTET)
    );
CREATE TABLE PLANWORK(
    PW INT NOT NULL,
    FAKYLTET VARCHAR(30) NOT NULL,
    DATA DATE,
    PRIMARY KEY(PW)
    );
ALTER TABLE bznosu ADD FOREIGN KEY (KPS) REFERENCES teahstud(KSP) ON DELETE CASCADE ON UPDATE RESTRICT;
ALTER TABLE teahstud ADD FOREIGN KEY (FAKYLTET) REFERENCES fakyltet (FAKYLTET) ON DELETE CASCADE ON UPDATE RESTRICT; 
ALTER TABLE teahstud ADD FOREIGN KEY (STATYS) REFERENCES statys (STATYS) ON DELETE CASCADE ON UPDATE RESTRICT; 
ALTER TABLE teahstud ADD FOREIGN KEY (DOLGNOST) REFERENCES dolgnost (DOLGNOST) ON DELETE CASCADE ON UPDATE RESTRICT;
ALTER TABLE planwork ADD FOREIGN KEY (FAKYLTET) REFERENCES fakyltet (FAKYLTET) ON DELETE CASCADE ON UPDATE RESTRICT; 
  